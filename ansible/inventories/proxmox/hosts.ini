# Proxmox VE VMs — for production
# Usage: ansible-playbook -i inventories/proxmox playbooks/setup-node.yml

[three_ds_nodes]
# Add your Proxmox VM hostnames or IPs here.
# Example:
# node01 ansible_host=192.168.1.101 ansible_user=cyokozai

[three_ds_nodes:vars]
ansible_python_interpreter=/usr/bin/python3
